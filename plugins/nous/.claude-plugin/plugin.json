{
  "name": "nous",
  "version": "0.4.0",
  "description": "The revenue layer for your coding agent — turn calls and emails into a live, ICP-scored account graph, extracted on your own tokens and filed to Nous.",
  "author": {
    "name": "OpenNous",
    "url": "https://opennous.cloud"
  },
  "homepage": "https://opennous.cloud",
  "repository": "https://github.com/NousC/nous-plugin",
  "keywords": ["revenue", "crm", "sales", "gtm", "icp", "mcp", "context-graph"],
  "userConfig": {
    "api_key": {
      "type": "string",
      "title": "Nous API key",
      "description": "Run /nous:nous-login to sign in with your browser (no copy-paste), or paste a key from https://app.opennous.cloud/connect/api-keys. Workspace-scoped.",
      "sensitive": true,
      "required": false
    }
  },
  "mcpServers": {
    "nous": {
      "command": "npx",
      "args": ["-y", "@opennous/mcp"],
      "env": {
        "NOUS_SURFACE": "plugin",
        "NOUS_API_KEY": "${user_config.api_key}"
      }
    }
  }
}
